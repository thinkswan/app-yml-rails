source :rubygems

gemspec

# The following gems are only used in the dev/test environments
group :development, :test do
  gem 'pry' # Allows us to call 'binding.pry' within any controller (to inspect current state)
end
